# SULTAN 0.7.1 acceptance gate

Do not merge this branch unless CI passes JavaScript syntax checks, engine tests, 0.7 hardening tests, 0.7.1 regression tests, all locale-catalog parity checks, the standalone build, and the Arabic/English browser suites.

Manual review after CI should inspect Arabic and English leadership/internal reports, the Value × Authority matrix at 0 and 100, one option with missing authority data, one cost criterion polarity change, and an escalation pack with both internal and external/shared unresolved decisions.
