# Final verified public beta scope

This release surfaces SULTAN as a decision-accountability system for strategy and includes the named SULTAN Decision Accountability Method, its five constructs, R1–R5 advisory coherence hints, public evidence visuals, open suggestion lists, Vision 2030 reference suggestions, progressive disclosure, import/cache correctness hardening, bilingual Arabic/English copy, and local-first privacy behavior.

## Rev-3 handoff closure

The findings in the rev-3 team handoff were re-measured on the current code and the remaining software items were closed:

- the two repetitive homepage bands were removed;
- the five evidence views now share one accessible tabbed stage while remaining in the DOM and directly addressable;
- the comparison between conventional planning and SULTAN is integrated into the methodology section instead of occupying a separate band;
- the four-step quick journey is merged into the closing CTA;
- mobile hero content is progressively disclosed so the primary action appears before the deeper preview;
- real-browser guards now enforce homepage length ceilings (<= 5.5 screens at 1440×900 and <= 9 screens at 390×844) and >= 4.5 interactive-element contrast above the fold;
- the fictional university example uses specific accountable role owners and a dated, quantified operating commitment;
- R1–R5 now use the canonical rule numbering and the more precise R1 condition: the numeric target must appear literally in the linked reference while that reference is not approved for use/adaptation;
- the import/check cache collision regression remains guarded by a content fingerprint.

The authoritative public deployment is the Render static site. GitHub Pages remains disabled for this repository and is not treated as the production route.

The approved logo asset remains the current supplied raster. A higher-resolution or SVG replacement should only be made when an approved original is provided; no substitute mark should be invented.
