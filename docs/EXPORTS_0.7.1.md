# Export surfaces in 0.7.1

The workspace presents report/section export actions through a consolidated Export / Share menu. Project-level JSON save/import remains the durable local working format; strategy, internal, leadership, escalation, and section exports serve different audiences and should be labeled accordingly in the UI.
